server:
  port: 8081

spring:
  application:
    name: configserver

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Aravind100-dot/microservices-demo.git
          clone-on-start: true
          default-label: main
          # search-paths removed, default is root
          
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
